# Copy to .env and fill in real values.
# Shared secret the Wazuh hook uses to sign alert payloads (HMAC-SHA256).
AGENTIC_SOC_HMAC_SECRET=change-me
# Minimum Wazuh rule level to analyze.
AGENTIC_SOC_MIN_RULE_LEVEL=9
